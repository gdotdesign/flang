# Comments
Comments are similar to [CSS Comments](http://www.w3.org/TR/CSS21/syndata.html#comments) they begin with the characters  `/*` followed by the **contents** and end with the characters `*/`.

```
/* main body */
body {
  ...
}
```
